services:
  app:
    build: ./  # Construye la imagen usando el Dockerfile en el directorio actual
    depends_on:
      - mysql
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: bd_bussiness_app
      DB_USERNAME: UNT
      DB_PASSWORD: pipipiG5UNT
    ports:
      - "8000:8000"  # Exponer el puerto 8000 del contenedor al host
    restart: always
    command: >
      /bin/bash -c "
      # Espera a que la aplicación esté completamente cargada
      while ! curl -s http://localhost:8000 > /dev/null; do
        echo 'Esperando que la aplicación esté disponible...';
        sleep 5;
      python3 /app/database.py;
      exec uvicorn main:app --host 0.0.0.0 --port 8000
      "

  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: pipipiG5UNT  # Contraseña del usuario root
      MYSQL_DATABASE: bd_bussiness_app  # Base de datos inicial
      MYSQL_USER: UNT                   # Nuevo usuario a crear
      MYSQL_PASSWORD: pipipiG5UNT       # Contraseña del nuevo usuario
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql  # Añadir volumen persistente para los datos de MySQL
    restart: always

 # phpmyadmin:
    #image: phpmyadmin:latest
    #ports:
      #- "9001:80"
    #environment:
      #PMA_HOST: mysql
     # MYSQL_ROOT_PASSWORD: pipipiG5UNT
    #restart: always

volumes:
  mysql_data:  # Definir el volumen persistente
